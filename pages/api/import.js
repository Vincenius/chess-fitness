// import fs from 'fs'
// import { initOpenings } from '../../lib/database'

// temp function to import openings to database
export default async function (req, res) {
  // const data = JSON.parse(fs.readFileSync(process.cwd() + '/lib/openings/1.json'))
  // await initOpenings(data)

  res.status(200).json({ })
}
